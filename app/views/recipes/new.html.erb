<%= render 'form', recipe: @recipe %>

<%= form_for @recipe do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %><br>

  <%= f.fields_for :ingredients do |i| %>
    <%= addr.label :name %>
    <%= addr.text_field :name %><br>

    <%= addr.label :quantity %>
    <%= addr.text_field :quantity %><br>
  <% end %>

  <%= f.submit %>
<% end %>
